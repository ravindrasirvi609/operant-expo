"use client";

import { useEffect, useMemo, useState } from "react";

type Organization = { _id: string; name: string };
type Booking = { _id: string; bookingNumber: string; status: string; stallId: string; exhibitorId: string; commercialSnapshot: { total: number; currency: string }; createdAt: string };

export default function BookingsPage() {
  const [organizations, setOrganizations] = useState<Organization[]>([]); const [organizationId, setOrganizationId] = useState(""); const [bookings, setBookings] = useState<Booking[]>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(""); const [filter, setFilter] = useState("ALL");
  async function load(id: string) { setLoading(true); setError(""); try { const response = await fetch(`/api/organizations/${id}/bookings`); const data = await response.json(); if (!response.ok) throw new Error(data.error ?? "Unable to load bookings"); setBookings(data.bookings); } catch (cause) { setError(cause instanceof Error ? cause.message : "Unable to load bookings"); } finally { setLoading(false); } }
  useEffect(() => { fetch("/api/me/organizations").then((response) => response.json()).then((data) => { setOrganizations(data.organizations ?? []); const first = data.organizations?.[0]; if (first) { setOrganizationId(first._id); void load(first._id); } else setLoading(false); }).catch(() => { setError("Unable to load organizations"); setLoading(false); }); }, []);
  async function updateStatus(bookingId: string, status: "CONFIRMED" | "CANCELLED") { const response = await fetch(`/api/organizations/${organizationId}/bookings/${bookingId}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ status }) }); const data = await response.json(); if (!response.ok) return setError(data.error ?? "Unable to update booking"); setBookings((current) => current.map((booking) => booking._id === bookingId ? data.booking : booking)); }
  const filtered = useMemo(() => filter === "ALL" ? bookings : bookings.filter((booking) => booking.status === filter), [bookings, filter]);
  const total = bookings.reduce((sum, booking) => sum + (booking.commercialSnapshot?.total ?? 0), 0);
  return <main className="mx-auto max-w-7xl px-6 py-10"><div className="flex flex-col gap-4 border-b pb-6 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-medium text-indigo-600">Organizer workspace</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Bookings</h1><p className="mt-2 text-zinc-500">Review reservations and manage commercial status.</p></div><select aria-label="Organization" value={organizationId} onChange={(event) => { setOrganizationId(event.target.value); void load(event.target.value); }} className="rounded-lg border bg-white px-3 py-2">{organizations.map((organization) => <option key={organization._id} value={organization._id}>{organization.name}</option>)}</select></div>{error && <p role="alert" className="mt-5 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<div className="mt-6 grid gap-4 sm:grid-cols-3"><div className="rounded-xl border bg-white p-5"><p className="text-sm text-zinc-500">Total bookings</p><p className="mt-2 text-3xl font-semibold">{bookings.length}</p></div><div className="rounded-xl border bg-white p-5"><p className="text-sm text-zinc-500">Pending action</p><p className="mt-2 text-3xl font-semibold">{bookings.filter((booking) => booking.status === "PAYMENT_PENDING").length}</p></div><div className="rounded-xl border bg-white p-5"><p className="text-sm text-zinc-500">Gross value</p><p className="mt-2 text-3xl font-semibold">{total.toLocaleString()}</p></div></div><div className="mt-8 flex flex-wrap gap-2">{["ALL", "PAYMENT_PENDING", "CONFIRMED", "CANCELLED"].map((value) => <button key={value} onClick={() => setFilter(value)} className={`rounded-full px-3 py-1.5 text-sm ${filter === value ? "bg-indigo-600 text-white" : "bg-zinc-100 text-zinc-600"}`}>{value.replace("_", " ")}</button>)}</div><section className="mt-4 overflow-hidden rounded-xl border bg-white">{loading ? <div className="animate-pulse p-8 text-zinc-500">Loading bookings…</div> : filtered.length === 0 ? <div className="p-10 text-center text-zinc-500">No bookings match this filter.</div> : <div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="border-b bg-zinc-50 text-zinc-500"><tr><th className="px-5 py-3">Booking</th><th className="px-5 py-3">Status</th><th className="px-5 py-3">Value</th><th className="px-5 py-3">Created</th><th className="px-5 py-3 text-right">Action</th></tr></thead><tbody className="divide-y">{filtered.map((booking) => <tr key={booking._id}><td className="px-5 py-4 font-medium">{booking.bookingNumber}</td><td className="px-5 py-4"><span className="rounded-full bg-zinc-100 px-2.5 py-1 text-xs">{booking.status}</span></td><td className="px-5 py-4">{booking.commercialSnapshot.total.toLocaleString()} {booking.commercialSnapshot.currency}</td><td className="px-5 py-4 text-zinc-500">{new Date(booking.createdAt).toLocaleDateString()}</td><td className="px-5 py-4 text-right">{booking.status === "PAYMENT_PENDING" && <div className="flex justify-end gap-2"><button onClick={() => void updateStatus(booking._id, "CONFIRMED")} className="rounded border border-green-200 px-2.5 py-1 text-xs text-green-700">Confirm</button><button onClick={() => void updateStatus(booking._id, "CANCELLED")} className="rounded border border-red-200 px-2.5 py-1 text-xs text-red-700">Cancel</button></div>}</td></tr>)}</tbody></table></div>}</section></main>;
}

