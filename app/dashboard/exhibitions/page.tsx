"use client";
import { useEffect, useState } from "react";
type Org={_id:string;name:string};type Exhibition={_id:string;name:string;slug:string;lifecycle:string;startDate:string;endDate:string};type Hall={_id:string;name:string;code:string;width:number;height:number};type Venue={_id:string;name:string;city?:string};
export default function Exhibitions(){const[orgs,setOrgs]=useState<Org[]>([]),[orgId,setOrgId]=useState(""),[exhibitions,setExhibitions]=useState<Exhibition[]>([]),[venues,setVenues]=useState<Venue[]>([]),[halls,setHalls]=useState<Hall[]>([]),[selected,setSelected]=useState<Exhibition>(),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[error,setError]=useState(""),[notice,setNotice]=useState("");async function load(id:string){if(!id)return;setLoading(true);try{const[a,b]=await Promise.all([fetch(`/api/organizations/${id}/exhibitions`),fetch(`/api/organizations/${id}/venues`)]);const ad=await a.json(),bd=await b.json();if(!a.ok||!b.ok)throw Error(ad.error??bd.error);setExhibitions(ad.exhibitions);setVenues(bd.venues)}catch(e){setError(e instanceof Error?e.message:"Unable to load workspace")}finally{setLoading(false)}}useEffect(()=>{fetch("/api/me/organizations").then(r=>r.json()).then(d=>{setOrgs(d.organizations??[]);if(d.organizations?.[0]){setOrgId(d.organizations[0]._id);void load(d.organizations[0]._id)}else setLoading(false)}).catch(()=>setError("Unable to load organizations"))},[]);async function select(e:Exhibition){setSelected(e);const r=await fetch(`/api/organizations/${orgId}/exhibitions/${e._id}/halls`),d=await r.json();if(r.ok)setHalls(d.halls);else setError(d.error)}async function lifecycle(e:Exhibition,next:string){if(!orgId)return;setSaving(true);setError("");setNotice("");const r=await fetch(`/api/organizations/${orgId}/exhibitions/${e._id}/lifecycle`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({lifecycle:next})});const d=await r.json();if(!r.ok)setError(d.error??"Unable to update status");else{setExhibitions(c=>c.map(x=>x._id===e._id?d.exhibition:x));if(selected?._id===e._id)setSelected(d.exhibition);setNotice(`${e.name} is now ${next.toLowerCase().replace("_"," ")}.`)}setSaving(false)}async function submit(event:React.FormEvent<HTMLFormElement>,path:string,done:(d:Record<string,unknown>)=>void){event.preventDefault();setSaving(true);setError("");try{const r=await fetch(path,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(event.currentTarget)))}),d=await r.json();if(!r.ok)throw Error(d.error??"Request failed");done(d);event.currentTarget.reset()}catch(e){setError(e instanceof Error?e.message:"Request failed")}finally{setSaving(false)}}return <main className="mx-auto max-w-7xl px-6 py-10"><div className="flex flex-col gap-5 border-b pb-8 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-semibold uppercase tracking-widest text-indigo-600">Organizer workspace</p><h1 className="mt-2 text-4xl font-semibold tracking-tight">Exhibitions</h1><p className="mt-3 text-zinc-500">Create events, publish them, and open your stall booking experience.</p></div><select value={orgId} onChange={e=>{setOrgId(e.target.value);void load(e.target.value)}} className="rounded-xl border bg-white px-4 py-3 text-sm dark:bg-zinc-900">{orgs.map(o=><option key={o._id} value={o._id}>{o.name}</option>)}</select></div>{error&&<p role="alert" className="mt-5 rounded-xl bg-red-50 p-4 text-sm text-red-700">{error}</p>}{notice&&<p role="status" className="mt-5 rounded-xl bg-emerald-50 p-4 text-sm text-emerald-700">{notice}</p>}{loading?<p className="mt-8 text-zinc-500">Loading exhibitions…</p>:<div className="mt-8 grid gap-8 xl:grid-cols-[1fr_360px]"><section><div className="mb-4 flex items-center justify-between"><div><h2 className="text-xl font-semibold">Your exhibitions</h2><p className="mt-1 text-sm text-zinc-500">Manage publication and booking availability.</p></div><span className="rounded-full bg-zinc-100 px-3 py-1 text-xs dark:bg-zinc-800">{exhibitions.length} total</span></div>{exhibitions.length===0?<div className="rounded-2xl border border-dashed p-12 text-center text-zinc-500">No exhibitions yet. Create one from the panel.</div>:<div className="space-y-3">{exhibitions.map(e=><div key={e._id} className={`rounded-2xl border bg-white p-5 shadow-sm dark:bg-zinc-900 ${selected?._id===e._id?"border-indigo-500 ring-4 ring-indigo-500/10":"dark:border-zinc-800"}`}><button onClick={()=>void select(e)} className="w-full text-left"><div className="flex items-start justify-between gap-4"><div><h3 className="text-lg font-semibold">{e.name}</h3><p className="mt-1 text-sm text-zinc-500">/{e.slug}</p></div><Status value={e.lifecycle}/></div><p className="mt-4 text-sm text-zinc-500">{new Date(e.startDate).toLocaleDateString()} – {new Date(e.endDate).toLocaleDateString()}</p></button><div className="mt-5 flex flex-wrap gap-2 border-t pt-4 dark:border-zinc-800">{e.lifecycle==="DRAFT"&&<button disabled={saving} onClick={()=>void lifecycle(e,"PUBLISHED")} className="rounded-lg bg-indigo-600 px-3 py-2 text-xs font-semibold text-white disabled:opacity-50">Publish exhibition</button>}{e.lifecycle==="PUBLISHED"&&<button disabled={saving} onClick={()=>void lifecycle(e,"BOOKING_OPEN")} className="rounded-lg bg-emerald-600 px-3 py-2 text-xs font-semibold text-white disabled:opacity-50">Open booking</button>}{e.lifecycle==="BOOKING_OPEN"&&<button disabled={saving} onClick={()=>void lifecycle(e,"BOOKING_CLOSED")} className="rounded-lg bg-amber-600 px-3 py-2 text-xs font-semibold text-white disabled:opacity-50">Close booking</button>}{["PUBLISHED","BOOKING_OPEN","BOOKING_CLOSED"].includes(e.lifecycle)&&<a href={`/exhibitions/${e.slug}`} target="_blank" className="rounded-lg border px-3 py-2 text-xs font-semibold">View public page ↗</a>}</div></div>)}</div>}{selected&&<div className="mt-8 rounded-2xl border bg-white p-6 shadow-sm dark:bg-zinc-900"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-widest text-indigo-600">Hall setup</p><h2 className="mt-1 text-xl font-semibold">{selected.name}</h2></div><span className="text-sm text-zinc-500">{halls.length} halls</span></div>{halls.map(h=><div key={h._id} className="mt-4 flex flex-wrap items-center justify-between gap-3 rounded-xl border p-4 dark:border-zinc-800"><span className="text-sm font-medium">{h.name} <span className="text-zinc-400">· {h.code}</span></span><div className="flex gap-2"><a className="rounded-lg bg-indigo-600 px-3 py-2 text-xs font-semibold text-white" href={`/dashboard/exhibitions/${selected._id}/halls/${h._id}/map/setup?organizationId=${orgId}`}>Upload map</a><a className="rounded-lg border px-3 py-2 text-xs font-semibold" href={`/dashboard/exhibitions/${selected._id}/halls/${h._id}/map?organizationId=${orgId}`}>View map</a></div></div>)}<form className="mt-6 grid gap-3 border-t pt-6 sm:grid-cols-2 dark:border-zinc-800" onSubmit={e=>void submit(e,`/api/organizations/${orgId}/exhibitions/${selected._id}/halls`,d=>setHalls(c=>[...c,d.hall as Hall]))}><input name="name" required placeholder="Hall name" className="rounded-lg border p-3"/><input name="code" required placeholder="Hall code" className="rounded-lg border p-3"/><input name="width" required type="number" min="1" placeholder="Width" className="rounded-lg border p-3"/><input name="height" required type="number" min="1" placeholder="Height" className="rounded-lg border p-3"/><button className="rounded-lg bg-zinc-900 p-3 text-sm font-semibold text-white dark:bg-white dark:text-zinc-900 sm:col-span-2">Add hall</button></form></div>}</section><aside className="space-y-5"><Form title="Add venue" subtitle="Add the location for your event." onSubmit={e=>void submit(e,`/api/organizations/${orgId}/venues`,d=>setVenues(c=>[...c,d.venue as Venue]))} fields={["name","city","country"]} button="Create venue"/><Form title="Create exhibition" subtitle="Start with the essentials; refine it later." onSubmit={e=>void submit(e,`/api/organizations/${orgId}/exhibitions`,d=>setExhibitions(c=>[d.exhibition as Exhibition,...c]))} fields={["name","slug","shortDescription","startDate","endDate","timezone"]} button="Create exhibition"/></aside></div>}</main>}
function Status({value}:{value:string}){return <span className="rounded-full bg-indigo-50 px-3 py-1 text-xs font-semibold capitalize text-indigo-700 dark:bg-indigo-950 dark:text-indigo-300">{value.toLowerCase().replace("_"," ")}</span>}
function Form({title,subtitle,fields,onSubmit,button}:{title:string;subtitle:string;fields:string[];onSubmit:(e:React.FormEvent<HTMLFormElement>)=>void;button:string}){return <form onSubmit={onSubmit} className="space-y-3 rounded-2xl border bg-white p-6 shadow-sm dark:bg-zinc-900"><h2 className="font-semibold">{title}</h2><p className="text-sm text-zinc-500">{subtitle}</p>{fields.map(f=>f==="shortDescription"?<textarea key={f} name={f} placeholder="Short description" className="w-full rounded-lg border p-3"/>:<input key={f} name={f} required={!['city','country','shortDescription'].includes(f)} type={f.includes('Date')?"date":"text"} defaultValue={f==="timezone"?"Asia/Kolkata":undefined} placeholder={f.replace(/([A-Z])/g," $1")} className="w-full rounded-lg border p-3"/>)}<button className="w-full rounded-lg bg-indigo-600 p-3 text-sm font-semibold text-white">{button}</button></form>}
