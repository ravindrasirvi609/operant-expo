"use client";

import { useEffect, useState } from "react";

type Org = { _id: string; name: string }; type Exhibition = { _id: string; name: string }; type Hall = { _id: string; name: string }; type Element = { _id: string; label?: string };
type Stall = { _id: string; floorPlanElementId: string; stallNumber: string; section?: string; stallType: string; width: number; height: number; basePrice: number; currency: string; status: string; visibility: string; amenities: string[]; area: number };
const statuses = ["AVAILABLE", "PENDING", "BLOCKED", "HELD", "BOOKED"];
const types = ["STANDARD", "PREMIUM", "CORNER", "ISLAND", "RAW_SPACE", "SHELL_SCHEME"];

export default function StallsInventory() {
  const [orgs, setOrgs] = useState<Org[]>([]), [orgId, setOrgId] = useState(""), [exhibitions, setExhibitions] = useState<Exhibition[]>([]), [exId, setExId] = useState(""), [halls, setHalls] = useState<Hall[]>([]), [hallId, setHallId] = useState(""), [elements, setElements] = useState<Element[]>([]), [stalls, setStalls] = useState<Stall[]>([]), [editing, setEditing] = useState<Stall>(), [loading, setLoading] = useState(true), [error, setError] = useState(""), [message, setMessage] = useState("");
  useEffect(() => { fetch("/api/me/organizations").then((r) => r.json()).then((d) => { setOrgs(d.organizations ?? []); setOrgId(d.organizations?.[0]?._id ?? ""); }).catch(() => setError("Unable to load organizations")); }, []);
  useEffect(() => { if (!orgId) return; fetch(`/api/organizations/${orgId}/exhibitions`).then((r) => r.json()).then((d) => { setExhibitions(d.exhibitions ?? []); setExId(d.exhibitions?.[0]?._id ?? ""); }).catch(() => setError("Unable to load exhibitions")); }, [orgId]);
  useEffect(() => { if (!orgId || !exId) return; fetch(`/api/organizations/${orgId}/exhibitions/${exId}/halls`).then((r) => r.json()).then((d) => { setHalls(d.halls ?? []); setHallId(d.halls?.[0]?._id ?? ""); }).catch(() => setError("Unable to load halls")); }, [orgId, exId]);
  async function loadHall(id: string) { setHallId(id); if (!orgId || !exId || !id) return; setLoading(true); setError(""); try { const [a, b] = await Promise.all([fetch(`/api/organizations/${orgId}/exhibitions/${exId}/halls/${id}/stalls`), fetch(`/api/organizations/${orgId}/exhibitions/${exId}/halls/${id}/map-elements`)]); const ad = await a.json(), bd = await b.json(); if (!a.ok || !b.ok) throw Error(ad.error ?? bd.error); setStalls(ad.stalls ?? []); setElements(bd.elements ?? []); } catch (e) { setError(e instanceof Error ? e.message : "Unable to load hall"); } finally { setLoading(false); } }
  // The loader intentionally follows the selected hall; organization and exhibition are
  // already reflected in the selected hall list before this effect runs.
  // eslint-disable-next-line react-hooks/exhaustive-deps
  useEffect(() => { if (hallId) void loadHall(hallId); }, [hallId]);
  async function submit(event: React.FormEvent<HTMLFormElement>, stall?: Stall) { event.preventDefault(); setError(""); setMessage(""); const raw = Object.fromEntries(new FormData(event.currentTarget)); const body = { ...raw, width: Number(raw.width), height: Number(raw.height), basePrice: Number(raw.basePrice), amenities: typeof raw.amenities === "string" && raw.amenities ? raw.amenities.split(",").map((v) => v.trim()).filter(Boolean) : [] }; const url = stall ? `/api/organizations/${orgId}/exhibitions/${exId}/halls/${hallId}/stalls/${stall._id}` : `/api/organizations/${orgId}/exhibitions/${exId}/halls/${hallId}/stalls`; const r = await fetch(url, { method: stall ? "PATCH" : "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(body) }); const d = await r.json(); if (!r.ok) setError(d.error ?? "Unable to save stall"); else { setStalls((all) => stall ? all.map((s) => s._id === stall._id ? d.stall : s) : [...all, d.stall]); setEditing(undefined); setMessage(stall ? `Stall ${d.stall.stallNumber} updated.` : `Stall ${d.stall.stallNumber} added to inventory.`); if (!stall) event.currentTarget.reset(); } }
  const form = (stall?: Stall) => <form onSubmit={(e) => void submit(e, stall)} className="space-y-3">{!stall && <select name="floorPlanElementId" required className="w-full rounded-lg border bg-white p-3"><option value="">Select map stall element</option>{elements.map((e) => <option key={e._id} value={e._id}>{e.label ?? "Unnamed stall"}</option>)}</select>}<input name="stallNumber" required defaultValue={stall?.stallNumber} placeholder="Stall number e.g. A-12" className="w-full rounded-lg border p-3" /><input name="section" defaultValue={stall?.section} placeholder="Section e.g. A" className="w-full rounded-lg border p-3" /><select name="stallType" defaultValue={stall?.stallType ?? "STANDARD"} className="w-full rounded-lg border bg-white p-3">{types.map((v) => <option key={v} value={v}>{v.replace("_", " ")}</option>)}</select><div className="grid grid-cols-2 gap-3"><input name="width" required type="number" min="1" defaultValue={stall?.width} placeholder="Width" className="rounded-lg border p-3" /><input name="height" required type="number" min="1" defaultValue={stall?.height} placeholder="Height" className="rounded-lg border p-3" /></div><div className="grid grid-cols-2 gap-3"><input name="basePrice" required type="number" min="0" defaultValue={stall?.basePrice} placeholder="Base price" className="rounded-lg border p-3" /><input name="currency" required defaultValue={stall?.currency ?? "INR"} maxLength={3} placeholder="Currency" className="rounded-lg border p-3" /></div><input name="amenities" defaultValue={stall?.amenities?.join(", ")} placeholder="Amenities, comma separated" className="w-full rounded-lg border p-3" /><div className="grid grid-cols-2 gap-3"><select name="status" defaultValue={stall?.status ?? "AVAILABLE"} className="rounded-lg border bg-white p-3">{statuses.map((v) => <option key={v} value={v}>{v}</option>)}</select><select name="visibility" defaultValue={stall?.visibility ?? "PUBLIC"} className="rounded-lg border bg-white p-3"><option value="PUBLIC">PUBLIC</option><option value="PRIVATE">PRIVATE</option></select></div><div className="flex gap-2"><button className="flex-1 rounded-lg bg-indigo-600 p-3 font-semibold text-white">{stall ? "Save changes" : "Create stall"}</button>{stall && <button type="button" onClick={() => setEditing(undefined)} className="rounded-lg border px-4">Cancel</button>}</div></form>;
  return <main className="mx-auto max-w-7xl px-6 py-10"><div className="flex flex-col gap-5 border-b pb-8 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-semibold uppercase tracking-widest text-indigo-600">Inventory</p><h1 className="mt-2 text-4xl font-semibold tracking-tight">Stalls</h1><p className="mt-3 text-zinc-500">Manage pricing, visibility and live availability for every bookable stall.</p></div><div className="flex flex-wrap gap-2"><select value={orgId} onChange={(e) => setOrgId(e.target.value)} className="rounded-lg border bg-white px-3 py-2 text-sm">{orgs.map((o) => <option key={o._id} value={o._id}>{o.name}</option>)}</select><select value={exId} onChange={(e) => setExId(e.target.value)} className="rounded-lg border bg-white px-3 py-2 text-sm">{exhibitions.map((e) => <option key={e._id} value={e._id}>{e.name}</option>)}</select><select value={hallId} onChange={(e) => void loadHall(e.target.value)} className="rounded-lg border bg-white px-3 py-2 text-sm">{halls.map((h) => <option key={h._id} value={h._id}>{h.name}</option>)}</select></div></div>{error && <p role="alert" className="mt-5 rounded-xl bg-red-50 p-4 text-sm text-red-700">{error}</p>}{message && <p role="status" className="mt-5 rounded-xl bg-emerald-50 p-4 text-sm text-emerald-700">{message}</p>}<div className="mt-8 grid gap-8 xl:grid-cols-[1fr_360px]"><section className="overflow-hidden rounded-2xl border bg-white shadow-sm"><div className="border-b p-5"><h2 className="font-semibold">Stall inventory</h2><p className="mt-1 text-sm text-zinc-500">{stalls.length} configured · {stalls.filter((s) => s.status === "AVAILABLE").length} available</p></div>{loading ? <p className="p-8 text-zinc-500">Loading inventory…</p> : stalls.length === 0 ? <p className="p-10 text-center text-zinc-500">No stalls configured for this hall.</p> : <div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-zinc-50 text-zinc-500"><tr><th className="px-5 py-3">Stall</th><th className="px-5 py-3">Area</th><th className="px-5 py-3">Price</th><th className="px-5 py-3">Status</th><th className="px-5 py-3">Action</th></tr></thead><tbody className="divide-y">{stalls.map((s) => <tr key={s._id}><td className="px-5 py-4 font-medium">{s.stallNumber}<span className="ml-2 text-zinc-400">{s.section}</span></td><td className="px-5 py-4">{s.area}</td><td className="px-5 py-4">{s.basePrice.toLocaleString()} {s.currency}</td><td className="px-5 py-4"><span className="rounded-full bg-zinc-100 px-2.5 py-1 text-xs">{s.status}</span></td><td className="px-5 py-4"><button onClick={() => setEditing(s)} className="rounded-md border px-3 py-1.5 text-xs font-semibold">Edit</button></td></tr>)}</tbody></table></div>}</section><aside className="h-fit rounded-2xl border bg-white p-6 shadow-sm"><h2 className="font-semibold">{editing ? `Edit ${editing.stallNumber}` : "Add bookable stall"}</h2><p className="mb-4 mt-1 text-sm text-zinc-500">{editing ? "Update pricing, visibility or booking status." : "Link a map rectangle to a bookable inventory item."}</p>{form(editing)}</aside></div></main>;
}
